# Real-Time Quant Signal Engine Requirements
# Install with: pip install -r quant/requirements_quant.txt

# === Core (inherit from ../requirements.txt) ===
fastapi>=0.109.0
uvicorn>=0.27.0
yfinance>=0.2.36
pandas>=2.2.0
numpy>=1.26.0
requests>=2.31.0
aiohttp>=3.9.0
textblob>=0.18.0
vaderSentiment>=3.3.2
python-dotenv>=1.0.0
ta>=0.11.0

# === Quant Engine Specific ===
# Structured logging
structlog>=24.1.0

# Rich terminal UI (8-panel dashboard)
rich>=13.7.0

# Time series forecasting (ARIMA)
statsmodels>=0.14.0

# RSS feed parsing
feedparser>=6.0.10

# Async file I/O
aiofiles>=23.2.1

# Technical analysis (alternative/supplement to ta)
# pandas-ta>=0.3.14b  # Optional: uncomment if needed

# HTTP client (already in requirements.txt via httpx)
httpx>=0.26.0
